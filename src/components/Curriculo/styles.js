import { styled } from '../../styles';

export const CurriculoStyle = styled('button', {
    width: '100%',
    background: 'none',
    border: '1px solid',
    color: 'white',
    borderRadius: 16,
    padding: '1px 16px',

  a:{
    fontSize: 10,
    color: 'white',
  }
});